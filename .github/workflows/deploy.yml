name: Deploy MiroFish to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: LesFruit/.github/.github/workflows/deploy-to-production.yml@main
    with:
      service: mirofish
      port_mapping: '100.77.118.43:3000:3000'
      health_url: 'http://127.0.0.1:3000'
